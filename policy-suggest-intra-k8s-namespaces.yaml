APIVersion: 1
label: recipe:policy-suggest:secure_intra-k8s_namespaces
data:
  recipes:
    - name: Secure Intra-Kubernetes Namespaces
      description: Creates a Network Ruleset Policy to allow Kubernetes namespace traffic.
      label: recipe:policy-suggest:secure_intra-k8s_namespaces
      metadata:
        - "@aporeto:author=aporeto"
      associatedTags:
        - "aporeto:recipe:placement=policy-suggest"
        - "aporeto:recipe:placement=intra-namespace"
      deploymentMode: NamespaceUnique
      targetIdentities:
        - networkrulesetpolicy
      propagate: true
      longDescription: |-
        ### Secure intra-kubernetes namespaces

        This recipe will create a Network Ruleset Policy to secure new
        and existing Kubernetes namespaces (third-level namespaces).

      template: |-
        {{`
        APIVersion: 1
        data:
          networkrulesetpolicies:
          - name: secure_intra-k8s_namespaces
            protected: true
            propagate: true
            incomingRules:
            - action: Allow
              object:
              - - $identity=processingunit
              protocolPorts:
              - any
            outgoingRules:
            - action: Allow
              object:
              - - $identity=processingunit
              protocolPorts:
              - any
            subject:
            - - $identity=namespace
              - $type=Kubernetes
        `}}
